<div class="navbar navbar-inverse navbar-static-top">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
<!--
				<span class="icon-bar"></span>
-->
			</button>
			<% if user_signed_in? %>
				<% if Rails.env.development? %>
					<%= link_to "DEVELOPMENT", {controller: "home", action: "index"}, class: "navbar-brand" %>
				<% else %>
					<%= link_to "Daily Notes", {controller: "home", action: "index"}, class: "navbar-brand" %>
				<% end %>
			<% else %>
				Home
			<% end %>
				
		</div>
		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
				<% if user_signed_in? %>
					<li class="active">
						<%= link_to "Home", controller: "home", action: "index" %>
					</li>
					<li>
						<%= link_to "Tags", controller: "tags", action: "index"%>
					</li>
					<li>
						<%= link_to "New", controller: "notes", action: "new"%>
					</li>
				<% else %>
					<li class="active">Home</li>
					<li>Tags</li>
					<li>New</li>
				<% end %>
<!--
				<li>
					<a href="#contact">Contact</a>
				</li>
-->
			</ul>
		</div><!--/.nav-collapse -->
	</div>
</div>